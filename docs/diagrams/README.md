# Diagrams

| File | What it shows |
|---|---|
| [`architecture.md`](architecture.md) | Package-by-feature layout, where the seams are, what talks to what |
| [`ingestion-fsm.md`](ingestion-fsm.md) | The ingestion state machine (Mermaid + auto-generated PNG) |
| [`retrieval-pipeline.md`](retrieval-pipeline.md) | The retrieval `Stage` sequence by strategy (sequence diagram) |

The Mermaid versions render inline on GitHub. The FSM also has a PNG (`ingestion-fsm.png`) regenerated by `make diagram-states` from the same source declaration in `app/ingestion/state.py` — that's the artifact for slides and external docs.
